# 立刻游：多人协同本地短时出行 AI 助理

## 1. 产品创新与核心流程

立刻游面向周末家庭、朋友小聚的本地短时出行，核心口号是：一句话，所有人的出行需求，AI 全帮你搞定，不用你操心。

它解决四类高频痛点：
- 商量乱：微信群里意见分散，AI 把需求、投票和修改意见收敛到同一个协同页。
- 做攻略烦：保留高德 POI、路线、天气、LLM 解析能力，自动查地点、餐厅、路线和预算。
- 订东西麻烦：一键确认后进入模拟执行，覆盖买票、订座、配送和分享消息。
- 突发情况慌：守护页持续盯天气、路况、商家状态，异常时给替代方案。

与传统推荐页不同，前端默认是聊天式界面。用户像发微信一样输入一句需求，例如“今天下午有空，带老婆孩子出去玩，别离家太远，老婆最近在减肥”，AI 返回 3 张极简方案卡片：方案名、时间线、预算、距离、标签和选择按钮。选中后可生成协同链接，同行人免登录投票和提意见，AI 再自动调整。

核心流程：
1. 用户一句话输入全员需求。
2. AI 判断信息是否足够；不足则在聊天里澄清。
3. 信息完整后调用真实 POI、路线、天气和可选网页校验，生成 3 套方案。
4. 用户分享给同行人，协同页支持投票和意见收集。
5. 用户一键确认，系统模拟买票、订座、配送和生成分享文案。
6. 出行当天守护页展示天气、路况、商家状态；下雨、满位、停业时提示替代方案。

## 2. Planning 策略、工具链路与异常处理

Planning 仍保留原有严肃约束：缺少可执行地点、明确开始时间、时长/结束时间、同行人、预算、核心需求时，不调用 POI、路线、天气或搜索工具，先返回澄清卡片。

工具链路：
- IntentParserAgent：解析自然语言需求，失败时用规则兜底。
- ClarificationService：判断必填信息、合并用户补充答案。
- AmapPoiSearchTool：查询真实高德 POI，普通运行不伪造地点。
- AmapRouteEstimateTool：估算路线距离和时间。
- AmapWeatherTool：天气为可选增强，失败只做 warning。
- SearchVerifierAgent：可选网页校验，失败不阻断核心方案。
- CollaborationMockService：新增 Mock 协同、全员记忆、出行守护 API，用于产品闭环演示。

异常处理：
- API 超时/错误：前端显示“刚刚网络有点小问题，要不要再试一次？”，不展示 HTTP 500。
- POI 不足：提示扩大范围、放宽低卡/距离/时间要求。
- 商家满位或停业：执行页和守护页展示“已更换同类型餐厅/室内乐园”的替代动作。
- 天气突变：守护页提示切换室内地点，并自动通知所有人。
- 会话过期：前端 API 客户端会清理旧 token、重建会话并重试一次。

当前新增 API 为 Mock 层，不替代真实规划链路：
- `POST /api/collab/shares`
- `POST /api/collab/shares/{shareId}/votes`
- `POST /api/collab/shares/{shareId}/comments`
- `GET /api/memory`
- `GET /api/guard/status`
